{
  "name": "langide-web",
  "version": "0.1.0",
  "description": "Web build of LangIDE — natural-English programming language in the browser.",
  "author": "Jaden Butler",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "deploy:j4den": "node build.mjs && node scripts/deploy-to-j4den.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
